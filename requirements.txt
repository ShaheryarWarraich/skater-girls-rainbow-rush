feedparser==6.0.11
httpx==0.27.2
PyYAML==6.0.2
Jinja2==3.1.4
pydantic==2.9.2

# Vendor SDKs are optional at runtime. The curation layer imports them lazily and
# degrades to the next provider (or the heuristic ranker) if one is missing, so a
# minimal install can still build the full site.
anthropic>=0.116,<1 ; python_version >= "3.9"
openai>=1.50 ; python_version >= "3.9"
